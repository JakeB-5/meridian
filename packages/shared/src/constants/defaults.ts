/** Default page size for paginated queries */
export const DEFAULT_PAGE_SIZE = 25;

/** Maximum rows returned from a single query */
export const MAX_QUERY_ROWS = 10_000;

/** Default query timeout in milliseconds */
export const DEFAULT_QUERY_TIMEOUT_MS = 30_000;

/** Maximum query timeout in milliseconds */
export const MAX_QUERY_TIMEOUT_MS = 300_000;

/** Default cache TTL in seconds */
export const DEFAULT_CACHE_TTL_SECONDS = 300;

/** Maximum dashboard columns */
export const MAX_DASHBOARD_COLUMNS = 24;

/** Default dashboard row height in pixels */
export const DEFAULT_ROW_HEIGHT = 80;

/** Minimum card width in grid units */
export const MIN_CARD_WIDTH = 2;

/** Minimum card height in grid units */
export const MIN_CARD_HEIGHT = 2;

/** Maximum card width in grid units */
export const MAX_CARD_WIDTH = 24;

/** Maximum card height in grid units */
export const MAX_CARD_HEIGHT = 20;

/** Default number of dashboard layout columns */
export const DEFAULT_DASHBOARD_COLUMNS = 12;

/** Maximum number of cards per dashboard */
export const MAX_CARDS_PER_DASHBOARD = 50;

/** Password minimum length */
export const MIN_PASSWORD_LENGTH = 8;

/** Password maximum length */
export const MAX_PASSWORD_LENGTH = 128;

/** Maximum SQL query length */
export const MAX_SQL_LENGTH = 50_000;

/** Maximum name length */
export const MAX_NAME_LENGTH = 255;

/** Maximum description length */
export const MAX_DESCRIPTION_LENGTH = 2000;
